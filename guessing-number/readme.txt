// if , else if, else statement
// ternary operator

// switch 
// string methods
// let name = 'jo-ash-u-a'
// console.log(name.charAt(2)) // get the character in the string
// console.log(name.indexOf('a'))  //gets the index of a string
// console.log(name.lastIndexOf("a")) //gets the last character 
// console.l
og(name.length) //gets the length of the string
// console.log(name.trim()) //removes any whitespace around
// console.log(name.toLowerCase()) //change from lower case
// console.log(name.toUpperCase()) //change from the upper case
// console.log(name.repeat(4)) //repeats a string
// console.log(name.startsWith('j')) //determine the first character , also usefull in conditional statement
// console.log(name.endsWith('a')) // determine the end character
// console.log(name.includes(' ')) // determine if the specified character contains those, also usefull in conditional statement 
// console.log(name.replaceAll('-', ' ')) // replaces all specified character to another type
// console.log(name.padStart(31,"0")) or console.log(name.padEnd(31,"0")) //adds character to after the 31 is reach

//string slicing = the process of creats a substring
//slice method
//if you use negative index it starts with the end

// let fullname = 'joshua daliva'
// // let firstName = fullname.slice(7) // starts with the specified index then overboard to the last  
// // firstName = fullname.slice(2,5) // specified range
// // let firstname = fullname.slice(0, fullname.indexOf(" ")) //gets the index
// console.log(firstName )


//method chaining - the process of calling one method after another 

// let userName = ' joshUa'
// let chain = userName.trim().charAt(0).toUpperCase() + userName.trim().slice(1).toLowerCase()



//logical operator use to combine or manipulate boolean values  AND = && OR = || NOT = !
//while loop and do while loop
//for loop

