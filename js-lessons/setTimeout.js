// setTimeout is a function used to execute a piece of code after a specified delay. It takes two main arguments:

//     1. The callback function that you want to execute after the delay.
//     2. The delay in milliseconds (1 second = 1000 milliseconds) before executing the callback function.
	
// Here's the basic syntax:
// setTimeout(function, delay);



// other methods of setTimeout

// 1. setTimeout()
// setTimeout(() => {console.log('haha')} ,1000)


// 2. clearTimeout()
// const timeoutID = setTimeout(() => {console.log('hello')} , 3000)
// clearTimeout(timeoutID)


// 3. setInterval()
// setInterval(() => {console.log('hello')} , 1000)


// 4. clearInverval()
// const clear = setInterval(() => {console.log('haha')} , 1000)
// clearInterval(clear)

