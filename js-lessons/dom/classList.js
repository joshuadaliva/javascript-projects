// classList  is an elements used to interact with an element list of classes (css classes)

//add()
//remove()
//toggle(remove if present , add if not)
//replace(old,new)
//contains


const btn = document.getElementById('btn');
btn.classList.add('enabled')